services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: dkb
      POSTGRES_PASSWORD: dkb_dev
      POSTGRES_DB: ai_store_dkb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
